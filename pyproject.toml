[project]
name = "mc-launcher"
version = "0.1.0"
description = "Cross-platform Minecraft launcher"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.6",
    "requests>=2.31",
]

[project.scripts]
mc-launcher = "main_qml:main"

[tool.setuptools.packages.find]
where = ["src/core/GUI/py"]
